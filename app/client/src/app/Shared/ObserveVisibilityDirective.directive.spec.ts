/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ObserveVisibilityDirectiveDirective } from './ObserveVisibilityDirective.directive';

describe('Directive: ObserveVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new ObserveVisibilityDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
